//Para sacar el volumen de un trapecio:
#include stdio.h
int main()
{
    int opcion;
    float Base, base, altura;
    printf("Dame el valor de la base");
    scanf("%f", &base);
    printf("Dame el valor de la Base");
    scanf("%f", &Base);

    volumen=(Base+base) /2
    printf("El valor del area es: %f, volumen");
    return 0;
}